<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.herba.sdk.jetpackexample">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".paging.MainPagingActivity"></activity>
        <activity
            android:name=".navigation.BottomNavigationActivity"
            android:label="@string/title_activity_bottom_navigation" />
        <activity android:name=".recyclerview.RecyclerViewActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".viewmodel.MainViewModelActivity" />
        <activity android:name=".livedata.MainLiveDataActivity" />
        <activity android:name=".livedatawithviewmodel.MainLiveDataViewModelActivity" />
        <activity android:name=".roomRecyclerview.MainRoomActivity" />
        <activity android:name=".roomRecyclerview.NoteAddActivity" />
        <activity android:name=".roomRecyclerview.NoteEditActivity" />
        <activity android:name=".navigation.SimpleNavigationActivity" />

        <receiver android:name=".paging.ItemBroadcastReceiver">
            <intent-filter>
                <action android:name="com.example.Broadcast" >
                </action>
            </intent-filter>
        </receiver>
    </application>

</manifest>